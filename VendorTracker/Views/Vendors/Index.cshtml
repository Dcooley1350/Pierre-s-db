@using VendorTracker.Models

<h1>Pierre's</h1>
<h2>Vendors:</h2>
@if(Model.Count == 0)
{
    <h3>No Vendors Here! Add some Vendors Pierre!</h3>
}
@foreach(Vendor item in Model)
{
    <h3><a href="vendor/@item.Id">@item.VenName</a></h3>
}
<p><a href="/vendor/new">Add Vendor</a></p>
<p><a href="/">Back Home</a></p>